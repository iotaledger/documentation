# Assembly transaction

An assembly is a committee of oracles.  An assembly is initiated by publishing an IOTA transaction with a special set of global parameters. This **assembly transaction** can subsequently be used by oracles to find a committee and to decide whether to join it.
